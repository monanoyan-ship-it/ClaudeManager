services:
  claude-manager:
    build: .
    container_name: claude-manager
    ports:
      - "41847:41847"
    volumes:
      - claude-manager-data:/app/data
    environment:
      - PORT=41847
      - DB_PATH=/app/data/claude_manager.db
    restart: unless-stopped

volumes:
  claude-manager-data:
